{{ form_start(form) }}
{{ form_widget(form, { 'attr': {'class': 'text-light col-6 mx-auto'} }) }}
<div class="d-flex justify-content-center">
	<button class="btn btn-primary col-lg-4">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
