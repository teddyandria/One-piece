<nav class="navbar navbar-expand-lg navbar-light" style="background-color: rgba(236, 201, 145, 0.7);">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home')}}"><img class="home_img" src="{{ asset('build/images/flag.png') }}" alt="One piece" width="50" height="40"/></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active text-light" aria-current="page" href="{{ path('app_home')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-light" href="{{path('crew_index')}}">Equipages</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-light" href="{{path('member_index')}}">Personnages</a>
				</li>

				{% if app.user != null %}
					<li class="nav-item justify-content-end">
						<a class="nav-link text-light" href="{{ path('app_logout')}}" tabindex="-1">Se déconnecter</a>
					</li>
				{% else %}
					<li class="nav-item justify-content-end">
						<a class="nav-link text-light" href="{{ path('app_register')}}" tabindex="-1">S'inscrire</a>
					</li>
					<li class="nav-item justify-content-end">
						<a class="nav-link text-light" href="{{ path('app_login')}}" tabindex="-1">Se connecter</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
