{% extends 'layout.html.twig' %}

{% block title %}Equipage
{% endblock %}

{% block stylesheets %}
	{{ encore_entry_link_tags('crew') }}
{% endblock %}

{% block body %}
	<div class="container">
		<div class="blockcrew d-flex mt-5">
			<h1 class="text-light text-center">{{crew.name}}</h1>
			{% for member in members %}
				<div>
					<img class="" src="{{ asset('build/images/' ~ member.crew.photo) }}" alt=" card image cap" width="150" height="150">
					<p class="text-light text-center">{{ crew.name }}</p>
				</div>
			{% endfor %}
		</div>
		<a href="{{ path('crew_index') }}">Retourner à la liste des équipages</a>
	</div>
{% endblock %}
